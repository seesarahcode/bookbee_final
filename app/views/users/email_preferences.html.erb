<% provide(:title, 'Email Preferences') %>

<h1>Email Preferences</h1>

<p> Receive emails when your books are rated or reviewed? </p>


<h3>Frequency</h3>


<h3>Follows</h3>

	<% if follows_exist?(current_user) %>
		<% @user = current_user %>
		<%= render "shared/index" %>

	<% else %>
		<p>Follow a book to receive rating and review notifications!</p>
	<% end %>


