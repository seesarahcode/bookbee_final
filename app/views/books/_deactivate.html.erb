<%= form_for(@book) do |f| %>
  <%= f.hidden_field :deactivated, :value => true %>
  <%= f.submit "Deactivate", data: { confirm: 'Are you sure you want to deactivate this book? It will no longer be able to receive ratings or reviews.' }, class: "btn btn-small btn-danger" %>
<% end %>
