<%= link_to gravatar_for(current_user), current_user %>
<h1>
  <%= current_user.name %>
</h1>
<span>
  <%= link_to "View profile", current_user %>
</span>
<span>
  <%= pluralize(current_user.books.count, "Book") %>
</span>