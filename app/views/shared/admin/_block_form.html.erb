<%= form_for([@user, @user.blocked_users]) do |f| %>
	<%= f.hidden_field :user_id, :value => @user.id %>
	<%= f.hidden_field :date_blocked, :value => Time.now %>
<%= f.submit "Block user", class: "btn btn-small btn-danger" %>
<% end %>
